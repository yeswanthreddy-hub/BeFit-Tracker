import PagePlaceholder from '../components/ui/PagePlaceholder'

function LoginPage() {
  return (
    <PagePlaceholder
      title="Welcome back"
      note="Signing in lives here. Local demo flow only — no backend."
    />
  )
}

export default LoginPage