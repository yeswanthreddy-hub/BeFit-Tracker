import PagePlaceholder from '../components/ui/PagePlaceholder'

function RegisterPage() {
  return (
    <PagePlaceholder
      title="Create your account"
      note="Registration lives here. Local demo flow only — no backend."
    />
  )
}

export default RegisterPage